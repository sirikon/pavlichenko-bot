version: '2'

services:

  pavlichenko-bot:
    image: pavlichenko-bot
    restart: always
    build:
      context: ../app
      dockerfile: ../docker/Dockerfile
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - DATA_FOLDER=/data
